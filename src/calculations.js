export const calculateInterests = (certificates) => {
  certificates.forEach((certificate) => {
    year = certificate.date.substring(0, 3);
    month = certificate.date.substring(5, 6);
    day = certificate.date.substring(8, 9);

    

    

  });
};
